import React from 'react';
interface SecondaryProps {
    expanded: boolean;
    children?: React.ReactNode;
    id?: string;
}
export declare function Secondary({ id, children, expanded }: SecondaryProps): JSX.Element;
export {};
